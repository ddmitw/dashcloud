server:
  port: 8080

spring:
  application:
    name: dash-gateway

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8767/eureka/

zuul:
  host:
    connect-timeout-millis: 60000
    socket-timeout-millis: 60000
  semaphore:
    max-semaphores: 500
  routes:
    sino-route:
      path: /api-system/**
      serviceId: dash-system